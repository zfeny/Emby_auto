# .env

# Rclone
# 远端名称，对应 `rclone config` 中配置的 remote 名称
RCLONE_REMOTE="123"
# 可选：全局附加参数，按需填入，例如 "--bwlimit 40M --retries 5"
RCLONE_GLOBAL_ARGS=""
# 可选：进度开关与参数（默认开启）
# RCLONE_PROGRESS=1
# RCLONE_PROGRESS_ARGS="--progress --stats 2s --stats-one-line"

# Files
# 对于单一目录
LOCAL_SOURCE_DIR="/path/to/source"
# 或者针对多个目录，使用分号/逗号/换行分隔
# LOCAL_SOURCE_DIRS="/path/to/dir1;/path/to/dir2"
# 如果需要为每个本地目录指定不同的远端目录，可使用映射配置（优先级最高）：
# LOCAL_REMOTE_MAPS="/file/123=>/123P/123;/file/456=>/123P/456"
RCLONE_REMOTE_ROOT="/remote/root"
